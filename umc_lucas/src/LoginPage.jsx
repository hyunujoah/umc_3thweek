import React from 'react';
import styled from 'styled-components';

// Styled Components
const Title = styled.h1`
  color: white;
  font-size: 3rem;
`;

const LoginPage = () => {
  return <Title>로그인 페이지</Title>;
};

export default LoginPage;