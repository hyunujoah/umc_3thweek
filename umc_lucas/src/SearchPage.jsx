import React from 'react';
import styled from 'styled-components';

// Styled Components
const Title = styled.h1`
  color: white;
  font-size: 3rem;
`;

const SearchPage = () => {
  return <Title>검색페이지 야호~!</Title>;
};

export default SearchPage;